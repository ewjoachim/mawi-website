variables:
  base_url:
    default: "https://manon-bc.fr/"
    from_env: URL

steps:
  - run: "npx tailwindcss -i content/_style.css -o output/style.css"
    watch:
      - "tailwind.config.js"
      - "content/_style.css"
      - "content/*.html"
